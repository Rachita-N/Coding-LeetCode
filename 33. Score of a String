class Solution {
    public int scoreOfString(String s) {
        int i,sum=0,k=0;
        for(i=0;i<s.length()-1;i++)
        { 
            
          sum+=Math.abs(s.charAt(i)-s.charAt(i+1));
          
        }
        return sum;
        
    }
}
